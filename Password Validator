print("create a new password that should contain 8 characters an 1 number")
def Password_Validator():
    password=input("create a new password: ")
    has_number=False
    for char in password:
        if char.isdigit():
            has_number=True
            
    if len(password)>=8 and has_number:
        print("u have entere a valied password")
    else:
        print("u entere a invalied password")
Password_Validator()
